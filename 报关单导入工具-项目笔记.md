# 报关单 PDF → Excel 智能导入工具

> 状态: ✅ 已完成 | 作者: MaZhenHai | 2026-06

---

## 📋 项目概述

纯前端网页工具，上传报关单 PDF 自动提取 32 个字段数据，填入表格并支持一键导出专业格式 Excel。无需服务器，数据 100% 本地处理。

## 🌐 访问地址

- **线上**: [http://91.209.228.214:8888/import_customs.html](http://91.209.228.214:8888/import_customs.html)
- **本地**: `/Volumes/存储/下载/报关单导入工具.html`
- **开发**: `~/Documents/视频二创/import_customs.html`

## 🚀 功能

### 核心能力
- 📤 拖拽或点击上传 PDF（支持批量多文件）
- 🔍 自动识别报关单 32 个字段 + 多项商品明细
- 📊 预览页表格实时展示识别结果
- ✅ 「已添加数据」标签页——选择性累积需要的行
- ⬇️ 导出专业格式 Excel（深蓝表头、斑马条纹、冻结首行、自动筛选）
- 🗑 逐行删除 / 全部清空 / 全部添加

### 提取字段 (32列)
`文件名` `境内发货人` `海关编码` `出境关别` `出口日期` `申报日期` `境外收货人` `运输方式` `运输工具名称及航次号` `提运单号` `监管方式` `合同协议号` `离境口岸` `包装种类` `件数` `毛重` `净重` `成交方式` `运费` `申报单位` `项号` `商品编号` `商品名称及规格型号` `数量` `单位` `单价` `总价` `原产国` `境内货源地` `最终目的国` `运费` `币制`

### 商品明细
- 支持多项商品自动识别（项号 1/2/3...按 y 坐标分区间提取）
- 第一法定单位优先（件 > 个 > 台 > ...）
- 自动合并规格型号多行数据

## 🏗 技术架构

```
CDN 依赖:
├── pdf.js 3.11.174       → PDF 文本 + 坐标提取
├── xlsx-js-style 1.2.0   → Excel 生成(带完整单元格样式)
└── 纯 HTML/CSS/JS         → 单文件, ~31KB

提取流程:
PDF 上传 → pdf.js getTextContent() → 坐标排序 → extractCustomsData()
  ├── 表头字段: 坐标范围定位 + 代码追加
  ├── 商品明细: 项号定位 → y区间分割 → 逐列解析
  └── 出口日期: 默认=申报日期

导出流程:
tableRows[] → exportStyledExcel() → xlsx-js-style → 下载 .xlsx
  ├── 表头: 宋体11pt 粗体白字 深蓝底 #2D5A87
  ├── 数据: 宋体10pt 偶数行 #F2F7FC 斑马纹
  ├── 对齐: 文字左/数字右/其余居中
  └── 功能: 首行冻结 + 自动筛选 + 逐列宽度
```

## 🖥 部署

| 项目 | 详情 |
|---|---|
| 服务器 | Ubuntu 24.04 LTS, nginx 1.24.0 |
| 主站 | `jiahao.abrdns.com` (Xboard :7001) |
| 工具端口 | 8888 (Python http.server 独立进程) |
| 路径 | `/var/www/html/import_customs.html` |
| 启动 | `cd /var/www/html && nohup python3 -m http.server 8888 --bind 0.0.0.0 &` |

## 🔧 关键技术决策

### 坐标容差策略
- pdf.js 与 PyMuPDF 坐标存在微小偏差
- 所有字段 y 范围放宽至 ±10~20pt
- 代码提取范围从 10pt 放宽至 20pt

### 标签排除规则
- 出境关别: 排除"出境""关别"避免误取标签行
- 境外收货人: 排除"有限""公司""("避免取到发货人信息
- 包装种类: 排除"包装""种类"标签文本

### 单位优先级
```js
unitPriority = ['件','个','台','套','辆','双','对',...]
// 第一法定单位优先, 千克归入净重
```

## 🐛 已修复问题记录

| 日期 | 问题 | 修复 |
|---|---|---|
| 06-08 | 出境关别不识别"梅山港区"(无"海关"二字) | 移除 `/海关/` 正则依赖，改用标签定位 + 排除关键词 |
| 06-08 | 多商品项只识别第一项 | y 范围从 280-320 扩展到 280+，追加去重逻辑 |
| 06-08 | 离境口岸/包装种类等取到标签文字 | 收紧 y 下限：160→168, 180→190 |
| 06-08 | 单位"个"覆盖"件" | 改为优先级系统，第一法定单位优先 |
| 06-08 | 出口日期为空 | 自动填入同申报日期 |
| 06-08 | 导出 Excel 与网页内容不一致 | 回退到标准 SheetJS CDN，直接写入 cells 数组 |
| 06-08 | `findValueByLabel` xMax 参数未生效 | `Math.abs(dx) < 100` → `< xMax` |
| 06-08 | 两个导出函数 130 行重复代码 | 提取 `exportStyledExcel()` 公共函数 |
| 06-08 | 已添加表格无 hover 效果 | 新增 `#accumTable` hover CSS |

## 📁 相关文件

| 文件 | 说明 |
|---|---|
| `import_customs.html` | 主程序 (单文件) |
| `import_customs.py` | Python CLI 版本 (备用) |
| `223123123123.xlsx` | 参考模板 (32列) |
| `EGLV143650916850H_报关单.pdf` | 测试 PDF 1 |
| `OOLU2166447020H_报关单.pdf` | 测试 PDF 2 |

## 🎯 使用场景

1. 外贸/物流公司日常报关单数据录入
2. 批量导入历史报关单到 Excel 归档
3. 报关数据分析（总价值/数量统计）
